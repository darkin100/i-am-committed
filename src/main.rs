use clap::Parser;
use colored::*;

#[derive(Parser)]
#[command(name = "iamcommitted")]
#[command(author = "Your Name")]
#[command(version = "1.0")]
#[command(about = "A command line application", long_about = None)]
struct Cli {
    
    

    
}

fn main() {
    

    //Get the git log

    //send to OpenRouter service

    //get the response



    println!("{}", "feat: Added new feature".green());
    
}
